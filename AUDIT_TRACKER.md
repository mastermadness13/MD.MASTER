# AUDIT_TRACKER.md

**هذا الملف هو الحالة الدائمة للمشروع عبر كل الجلسات.**
كل وكيل (Cursor / Claude Code) يبدأ جلسته بقراءة هذا الملف أولاً،
وينهي جلسته بتحديثه — قبل أي شي ثاني.

---

## قواعد عمل دائمة (تُطبَّق بكل جلسة، ليست "مهام" تُشطب)

هذي من الأقسام 1-4، 16، 17، 19-22 بالبرومبت الأصلي — تحكم *كيف* يشتغل
الوكيل، مو *ماذا* يفحص:

- لا تحوّل المشروع لتقنية أخرى (React, Django, إلخ) — Flask/Jinja2 يبقى كما هو
- حافظ على الواجهة العربية RTL
- افهم الكود الموجود قبل التعديل — لا تعيد الكتابة من الصفر بدون سبب موثّق
- التعديل الأصغر الآمن أفضل من إعادة الهيكلة الكبيرة
- لا تُضعف الاختبارات لتنجح — أصلح السبب الجذري
- عند التعارض بين الكود والمتطلبات: اشرح التعارض، لا تختر بصمت
- **لا تنه الجلسة بدون تحديث هذا الملف** — هذا أهم قاعدة جديدة
- الأدلة المرجعية الخارجية موجودة داخل `_audit_guides/` فقط، وفهرسها
  `GUIDES_MAP.md`. لا تفتح `github-pages-main/` (حُذف من المشروع) ولا
  تبحث في أي مجلد كبير آخر بحثاً عن مراجع.

---

## المجالات الفعلية (12 مجال) — هذي تُشطب واحد واحد

**قاعدة التنفيذ: مجال واحد فقط لكل جلسة. لا تنتقل لمجال ثاني بنفس الجلسة
حتى لو انتهيت مبكراً — استخدم الوقت المتبقي للتعمق أكثر بنفس المجال
بدل التوسع لمجال جديد.**

الترتيب أدناه هو ترتيب التنفيذ الفعلي (P0 أولاً بالكامل، ثم P1، ثم P2).

### P0 — حرج (أمان / بيانات) — يُنفَّذ أولاً، بالترتيب

| # | المجال | القسم بالبرومبت | الحالة | آخر تحديث | ملاحظات |
|---|---|---|---|---|---|
| 1 | مراجعة قاعدة البيانات (SQL injection, foreign keys, N+1, indexes) | §6 | **منتهي** | جلسة 2026-09-26 | **SQLi**: مسح ~120 استعلام f-string؛ كل القيم مربوطة بـ`?`، ومعرّفات الجداول ثابتة داخلياً؛ `build_order` في `database/repositories/_helpers.py:70` مقيد بـallowlist؛ `services/search.py` لا يستقبل `order` من الطلب أصلاً. لا ثغرة مؤكدة.<br>**FKs**: `configure_connection` (`database/connection.py:25`) يفعّل `PRAGMA foreign_keys = ON` و`flask_db.get_db()` يمر عبره؛ اتصالات `sqlite3.connect` الخام الوحيدة هي أمر CLI (`flask_db.py:113`)، ومخزن rate-limiter مستقل بلا FKs، وأدوات. قاعدة جديدة = 0 مخالفات. **مخالفتان في بيانات التطوير القديمة**: `course_content_submissions` صف 1 يشير إلى `users.id=1` غير الموجود (المستخدم حُذف) — سُجّل كقرار معلّق أدناه.<br>**N+1 (مُصلح)**: `services/course_content_service.py:387` (رُفع `PRAGMA table_info` خارج الحلقة + `executemany`)، `services/course_service.py:483` (استعلام واحد `GROUP BY course_id, semester`)، `services/exam_service.py:335` (استعلام واحد لكل الأقسام + تقسيم في Python).<br>**علة سابقة اكتُشفت وأُصلحت**: `copy_submission_as_draft` كان يستثني `submission_id` من INSERT فيرفع `IntegrityError` عند نسخ أي منهاج — طريق حيّ في `page_routes/teacher_pages.py:2003`.<br>**indexes (مُطبَّق)**: أضيف `idx_timetable_teacher_day` على `timetable(teacher_id, day)` في `schema.sql` و`_ensure_runtime_indexes` — يخدم استعلام الجدول الأسبوعي الأشيع، والخطة تغيّرت من `teacher_id=?` إلى `teacher_id=? AND day=?`. **رُفضت** بقية الفجوات بأدلة: الجداول صغيرة (timetable=80 صف) ولا `sqlite_stat1`، فلا يوجد قياس أداء ممكن على بيانات التطوير؛ و`deleted_at`/`status` أعمدة منخفضة التمايز. إن ظهر بطء في الإنتاج، فهذه المرشحة التالية. |
| 2 | المصادقة والتفويض (Auth & Authorization) | §7 | **منتهي** | جلسة 2026-09-26 | **نتيجة يفرضها الدليل** (`WEB-PROTOCOL-AND-AUTH.md`): السياسة الفعّالة = أضعف مسار متوازٍ، والباب لكل هوية. حُصر **255 مساراً**: صفر مسار غير عام يصله زائر — `enforce_permissions` يتخطى فحص user's `user_id` (`app.py:251`) فيتوقّف الأمن عند `login_required` لكل view، وأقوى فحص للقائمة أكّد صفراً (`0` unguarded)؛ توزيع البوابات: 214 `permission_required`، 5 `role_required`، 2 `any_role_required` (صفحتا ذاتية للمدرس)، 34 public/auth-only. **الاستنتاج المعاكس مهم**: `PUBLIC_PREFIXES` فيه `/uploads/` فيسقط `enforce_session_version` و`enforce_permissions` معاً — تبيّن المصدر أن العرض نفسه يحمل `@login_required` + `@permission_required('uploads.serve')` + فحص ملكية المجلد، فلم ثغرة. falsification هذه ضروريّة: الافتراض الأول كان خاطئاً))<br>**إدارة المفتاح**: لا fallback ثابت (env → `.secret_key` → عشوائي)؛ الملف غير متعقّب في git ولا في تاريخه، مُتجاهَل، 64 hex. **الجلسة كوكي موقّع على العميل** (لا Flask-Session) ⇒ مصداقية `roles` كلها = سرية المفتاح، وقد تحققت أنها سليمة.<br>**ثبت نظيف**: `session_dict.clear()` عند الدخول يُسقط session fixation؛ `update_password` يرفع `session_version` فعلاً (ادعاء `enforce_session_version` صحيح)؛ رموز الاستعادة **مُجزّأة في التخزين** (`_hash_reset_token`) + `used=0 AND expires_at>now` + إبطال عند الاستعمال؛ `scrypt`؛ `switch_role` يتحقق `requested in granted` + POST + CSRF؛ `core/wsgi_proxy.py` fail-safe (ProxyFix معطّل افتراضياً ومقصوراً على الوسطاء المُرحَّب بهم)؛ `auth_limits` **مشترك بين مساري الدخول** HTML وAPI (يمنع مضاعفة المحاولات) ومربوط بـIP **و**اسم المستخدم؛ `_check_teacher_access` يغلق IDOR (HOD → أعضاء قسمه فقط عبر `teacher_departments`، والمدرس → نفسه، وإلا 403). `teachers.manage` **حصري لـ`faculty_affairs`** — فرفض HOD مقصود. رموز الاستعادة مؤقتة 6 أرقام لكن 5 محاولات + 60 دقيقة +لمرة واحدة + إصدار يتطلب صلاحية ⇒ غير قابلة للكسر.<br>**مُصلح (مؤكَّد) — بقاء الصلاحيات بعد إبطالها**: `update_user` و`set_user_roles` و`add_user_role` و`remove_user_role` كانت **لا ترفع `session_version`** (الموقعان الوحيدان الرافعان: تغيير كلمة المرور والدخول برمز استعادة). فتنزيلُ دور من `faculty_affairs` إلى `teacher` كان يترك كوكي الجلسة الصادرة وهو يحمل الدور القديم، وصلاحياته كاملة، حتى انتهاء `PERMANENT_SESSION_LIFETIME` (7 أيام) — لأن `roles` تعيش في الكوكي. النتيجة: حساب منزوع الصلاحية يواصل حيازة `teachers.manage` (إعادة تعيين كلمة مرور أي عضو + سجل). أُصلح بدوران العدّاد في المسارات الخمسة + `set_user_active` في الاتجاهين. **تحقق عكسي**: 8 اختبارات تفشل بدون الإصلاح، منها اختبارا حدّ end-to-end، + حارس `ast` دائم يرفض أي method جديد يكتب صلاحية دون تدوير العدّاد.<br>**needs_validation — أصل رابط الاستعادة**: `url_for(..., _external=True)` (`auth_pages.py:134`) ينسخ `Host` header إلى رابط يحمل **رمز استعادة (bearer credential)**. **مُثبَت بفيقة محلية**: `Host: attacker.example` ⇒ `http://attacker.example/reset-password/TOKENVALUE`، ولا `SERVER_NAME` ولا قاعدة موثوقة. الإرسال نفسه مصمَّم بغير قابل للكسر (5/60 على IP واسم)، لذا الأثر = **تسميم الرابط**: المهاجم يستدعي الاستعادة باسم مستخدم الضحية بـHost مزوّر، فيصل الضحية بريداً من عنوان التطبيق الحقيقي برابط يشير لمضيف المهاجم؛ بالنقر تنتقل الرمز. سلسلة الاستغلال الكاملة تعتمد على النشر (هل الوسيط يتحقق من `Host`؟ `waitress` لا يفعل)، فتبقى `needs_validation` بلا severity حسب قواعد الدليل — لكن **جذر العلة مؤكَّد** والأصلح آمن. أُضيف `RESET_BASE_URL` (env) تُبنى منه الرابط عند ضبطه، مع,صيانة السلوك القديم + تحذير مسجَّل عند تركه فارغاً حتى لا تنكسر بيئة التطوير.<br>**ملاحظات (لا ثغرات، للعلم فقط)**: (1) رمز الاستعادة 6 أرقام ≈ 20 بت — مخفَّضة بالضوابط أعلاه، تُقوّى فقط لو أُريد. (2) `create_password_reset` لا يُبطل الرموز المستخدمة منتهية الصلاحية فقط ⇒ رموز غير مستخدمة تبقى صالحة بعد نجاح إعادة تعيين، وأثرها منخفض (الرموز مُجزّأة ولا تُكشف في الاستجابة، والإصدار يتطلب صلاحية). (3) `any_role_required` يتجاهل الدور النشط — مساران فقط لصفحتَي الذات، بلا أثر عابر للحدود. (4) `TRUSTED_PROXY_IPS` في `serve.py:21` يوثّق `10.0.0.0/8` لكن `core/wsgi_proxy.py:41` يطابق **نصاً حرفياً** ⇒ مثال موثّق لا يعمل. الاتجاه آمن (fail-safe: لا ثقة بالوسيط، فكل المستخدمين في جدول معدِّل واحد) لكنه مضلِّل ويحوّل المعدِّل إلى قفل دخول عام عندproxy خاطئ الإعداد. **القرار المطلوب**: أضيف دعم CIDR أم صحّح التوثيق؟ |
| 3 | الدفاع الأمني العام (XSS, CSRF, IDOR, رفع الملفات، إعدادات) | §8 | **منتهي** | جلسة 2026-09-26 | رؤوس الأمان: نُقلت من كتلة `after_request` داخل `app.py` إلى وحدة جديدة `security/security_headers.py` (مصدر حقيقة واحد قابل للتدقيق). **أُضيف HSTS** — كان مفقوداً تماماً، وهو الحماية الجديدة الحقيقية. **حُذفت** `X-XSS-Protection: 1; mode=block` (مهجورة منذ 2018، وفلترها القديم استُخدم لإحداث ثغرات CVE-2019-11358) وأُبقيت `0`. أُضيف لتوجيهات CSP الناقصة: `frame-src 'none'`, `worker-src`, `manifest-src`, `media-src` (تحققت: لا iframe، لا service worker، لا manifest، لا وسائط). أُضيف `assert_debug_disabled` (فشل عند الإقلاع لو Debug شغّال) و`block_scanner_probes` (حجب 18 مسار فحص آلي، exempt لـ`/static/` و`/uploads/`). 52 اختباراً جديداً؛ المجموع 515/515. **لم يُستخدم flask-talisman عن قصد** — انظر الملاحظة أدناه.<br>**XSS (تدقيق عميق):** فُحص 207 موضع بناء HTML في 37 ملف JS. الركائز السليمة: صفر `\|safe`، صفر `Markup()` في كود التطبيق، صفر `eval`/`new Function`/`document.write`، و`tojson` في `base.html` ينقل flash بترميز JSON لا HTML. الخطر الحقيقي كله في `innerHTML`/`insertAdjacentHTML`. **سلسلة stored DOM XSS مؤكدة:** `toast.js` كان يمرر `message` إلى `innerHTML`، و`base_flash.js` يغذّيه من `BASE_FLASH_BOOT.messages`، و`base.html:207` ينقل `flash()` عبر `tojson` (استعادة `<` قبل innerHTML)، ورسائل flash تحقن أسماء محفوظة (`teacher["name"]` في `page_routes/teachers.py`)، ولا يوجد أي `flash()` يحمل HTML مقصوداً — فالهروب عند الـtoast آمن. أُصلح 11 ملف: `toast.js` (أوسع sink — يغطي كل `flash()` في التطبيق)، `courses_list.js` (`d.name`, `c.requires`)، `teachers_assign.js`، `teachers_teaching_record_standalone_dept.js`، `faculty_performance_select_report.js`، `faculty_performance_edit_assignments.js` (أسوأ حالة: اسم نوع المهمة الحرّ يصل داخل `value="..."` — كسر الصفة ممكن، و`_ensure_admin_task` في `page_routes/teachers.py` لا ينظّف سوى `.strip()`)، `courses_create.js`، `timetable_live.js` (`currentYear` في فرعي الـpopup، والأصل باقٍ خاماً)، `exams/workspace.js` (تواريخ وفترات الامتحان)، `spa/app.js` و`shared/timetable_helpers.js` (كانا يفلتان `'` — الأخير وقّى كل عروض الجداول لأن `timetable_live.js` يستعير `H.esc`). **تدقيق `href`/`javascript:` نظيف:** كل الروابط مبنية من `url_for()` بمعرّفات صحيحة (`teacher_pages.py:165,176`، `timetable.py:651`) — لا يوجد رابط قابل للتحكم من المهاجم. 38 اختباراً جديداً في `tests/test_xss_output_escaping.py` (سلوكي عبر Node + ساكن)؛ المجموع 575/575.<br>**CSRF (مُغطّى، لا ثغرة)**: `tests/test_csrf_coverage.py` (٩ اختبارات) + ٦ اختبارات `switch_role` في `test_phase1_security_fixes.py` — كل مسار(state-changing) يحمل `csrf_required` أو تتحقق الرموز؛ `PUBLIC_PREFIXES` التي تحتوي `/uploads/` لا تشمل أي مسار كتابة.<br>**IDOR (مُغطّى عبر مجالات أخرى، لا تكرار)**: العزل مُغلق تجريبياً في #2 (255 مساراً، صفر غير محمي) وفي #4 (11 حارساً، تحقّق عكسي ٦/٦)؛ لا حاجة لإعادة اختباره هنا.<br>**الإعدادات**: `assert_debug_disabled`، ومفتاح سرّي بلا fallback ثابت، وكوكي الجلسة — كلها مُغطاة. **قرار معلّق**: `TRUSTED_PROXY_IPS` (CIDR أم نص حرفي) — انظر قائمة القرارات.<br>**رفع الملفات**: منفصل في **#5** (§10) وأُغلق هناك بنتيجة سالبة + فجوة دفاع-عميق موثّقة. |
| 4 | أمان منطق الأعمال (business-logic bypass) | §9 | **منتهي — نتيجة سالبة** | جلسة 2026-09-26 | **الجواب على سؤال المجال: لا. لا يصل HOD إلى بيانات قسم آخر على أي سطح فُحص.** الدليل `DATA-ISOLATION-AND-LIFECYCLE.md` («حقل `department_id` على السجل ليس عزلاً»). الأسطح المُتحقَّقة: `course_content_submissions` (عام=`published`، معلم=`AND teacher_id` من صف المعلم في الجلسة، super-admin=صلاحية)؛ مواد HOD (`department_id` مربوط في مسارَي القراءة والحذف معاً، فالـ`DELETE ... WHERE id = ?` بعده آمن)؛ `faculty_performance` (كلا مسارَي الطباعة `print/<teacher_id>` و`leaves/<teacher_id>/print` يستدعيان `_check_teacher_access`)؛ الجداول الزمنية (`department_id` في كل استعلام)؛ الاستيراد/الاستعادة (٢٦ مساراً، كلها `*.manage`)؛ الكتابة عبر API (`teachers.manage` لا `teachers.view`).<br>**نتيجة سالبة موثّقة — لا تُبلَّغ كثغرة**: `tests/test_teachers_list_visibility.py` ينصّ عمداً على أن كل حامل `teachers.view` — ومنهم HOD — يرى جدول الأساتذة كاملاً؛ فـ`teaching_record_print` تحت نفس الصلاحية **متّسق مع سياسة معلنة**، والدليل يشترط ذكر حدّ الوصول المؤسَّس قبل التبليغ لا بعده.<br>**هشاشة انحدار لا ثغرة**: `TeacherRepository` لا يفلتر `deleted_at` في أي دالة قراءة (٦/٦)، والالتزام في طبقة المسارات (~٤٠ موضعاً). **هذا مقصود** لأن `restore` يحتاج قراءة الصف المحذوف؛ الخلل لم يتحقق لأن كل مستهلك يبرّره. الخطر المستقبلي: مسار جديد بلا تومب.<br>**حارس انحدر (١١ اختباراً جديداً)**: `tests/test_business_logic_area4_isolation.py` — يقود المسار HTTP الحقيقي بمستأجرين وهميين (قسمان + HOD) بدل تحليل ساكن. **تحقّق عكسي ٦/٦**: حُذفت كل حماية بالتبادل فانهار الاختبار المقابل في ٦/٦. أُصلح أثناء ذلك اختبار دورة الحياة كان يمرّ **لسبب خاطئ** (صف بلا `filename`، فكان 404 من غياب الملف لا من بوابة `published`؛ أُضيف ضابط إيجابي يثبت أن البايت تُخدَم فعلاً). |
| 5 | أمان رفع الملفات تحديداً | §10 | **منتهي — نتيجة سالبة** | جلسة 2026-09-26 | **الجواب على سؤال المجال: لا. لا ملف مرفوع يعود كمحتوى نشط في متصفّح أي مستخدم.** حُصر **٩ مواضع `request.files`** في `page_routes/teachers.py` و`page_routes/teacher_pages.py`، وفُحص كل مسار كتابة إلى القرص. **الضوابط المتحقَّقة (٨)**: (١) **٣ قوائم allowlist** — `core/constants/uploads.py`، و`_PHOTO_EXTENSIONS` (`teachers.py:22`)، ومجموعة `_save_course_file` (PDF فقط) — ولا واحدة منها تقبل `html/svg/xml/js/php/exe`؛ (٢) **كل الأسماء المخزَّنة من uuid** (`teacher_pages.py:987`, `:243`، `_store_teacher_photo`) ⇒ لا مكوّن مسار من المستخدم، و`secure_filename` غير مستعمل لكنه غير ضروري لأن الاسم مُولَّد بالكامل؛ (٣) **`nosniff` عالمي** (`security/security_headers.py:106`) ⇒ لا استنتاج نوع المحتوى؛ (٤) `/uploads/` **يرفض `..` قبل بوابة الملكية** ويطبّع `\` لويندوز؛ (٥) **سقوف حجم**: 16MB عام (`config.py:51`) + 4MB للصور (`teachers.py:23`)؛ (٦) **لا استخراج ضغط** في أي مكان ⇒ لا zip-slip؛ (٧) **CRLF في `Content-Disposition` مرفوض عند الكتابة** (`ValueError` من Werkzeug 3.1.3) ⇒ لا حقن ترويسات؛ (٨) **فصل الصلاحيات**: `uploads.view` (رفع) ≠ `uploads.serve` (تنزيل) — دور `teacher` يملك الأول فقط.<br>**لماذا الامتدادات هي الحاجز كله**: لا شيء يفحص البايتات — تُقبل الصورة لأن *اسمها* ينتهي بـ`.jpg` لا لأنها JPEG. فسلامة النظام معلّقة على القوائم الثلاث، و`nosniff` + نوع المحتوى غير القابل للتنفيذ هو ما يوقف البايتات الخبيثة خلف اسم مسموح. هذا **مُثبَت سلوكياً لا مُدَّعى**: `test_html_bytes_behind_an_allowed_extension_are_served_as_inert_content` يرفع HTML كاملاً داخل `.jpg` و`.txt` ويُثبت أن الرد `image/jpeg`/`text/plain` مع `nosniff`.<br>**فجوة دفاع عميق — موثّقة، غير قابلة للاستغلال اليوم**: طبقة الخدمة `/uploads/` **لا تملك سياسة امتداد أو نوع محتوى خاصة بها**؛ تُخدِم أي ملف موجود في الشجرة بـ`200` ونوعه الأصلي `inline`. وCSP في التطبيق يحوي `script-src 'unsafe-inline'` (خطأ موثّق في وحدة `security_headers.py` نفسه)، فـSVG أو HTML داخل الشجرة **سينفّذ** — و`nosniff` لا يحمي هناك لأن SVG نوع وثيقة خاص به. أثبتُّ ذلك بزرع `.svg` يحمل سكربت على القرص: خُدِّم `200 image/svg+xml`. **غير قابل للاستغلال اليوم** لأن القوائم الثلاث تمنع إنتاج مثل هذا الملف، لكنه **حاجز واحد بلا نسخ**: أي تعديل مستقبلي على قائمة، أو كاتب ثانٍ في الشجرة، يعيد فتح stored XSS دون بوابة ثانية. **مُوثَّق كـ`xfail`** في `test_uploads_route_has_no_extension_policy_of_its_own` ليبقى ظاهراً. **القرار المطلوب**: هل نضيف حاجزاً مركزياً في `uploads.uploaded_file` (رفض الامتدادات النشطة + إجبار `as_attachment`)<br>**حارس انحدر (٢٧ اختباراً)**: `tests/test_file_upload_area3.py` — يقود المسار HTTP الحقيقي بمستأجرين وهميين (معلّم + HOD)، لأن الفصل غير متماثل عمداً: `teacher` يملك `uploads.view` دون `uploads.serve`، وHOD يملك الاثنين، واختبارٌ بواحد منهم كان ليمرّ لسبب خاطئ. يغطّي: القوائم الثلاث (١٨)؛ قرارات القبول/الرفض للصور (امتداد نشط، حجم، فارغ، اسم مُولَّد)؛ اجتياز المسار في `/teacher/upload` (٤)؛ اجتاز المسار وأنواعه في `/uploads/` (٧)؛ رفض مجلد المالك الآخر (١)؛ دور المعلّم لا يصل لخدمة الرفع (١)؛ وبلا-نزع-نوع-المحتوى (١).<br>**تحقّق عكسي ١٢/١٢**: حُذفت/وُسِّعت كل حماية بالتبادل فانهار الاختبار المقابل في ١٢/١٢. **والتحقق العكسي كشف حارسين زائفين كانا سينجحان دائماً**: (أ) مقارنة `EXECUTABLE_EXTENSIONS & _PHOTO_EXTENSIONS` كانت تُجرَّد النقاط من **الجهة الخطأ** فتصبح فارغة دائماً؛ (ب) محلّل قائمة المواد أنتج `"'html'"` فلم يطابق `"html"` لأن علامات التنصيص لم تُنزع. كلاهما مرّ في التشغيل الأول ووُجدا في الجولة الثانية. |

### P1 — عالي (وظائف / اختبارات)

| # | المجال | القسم بالبرومبت | الحالة | آخر تحديث | ملاحظات |
|---|---|---|---|---|---|
| 6 | مراجعة الـ Backend/Routes (Python quality, route structure) | §5 | قيد التنفيذ | (جلسة سابقة) | أُصلح خلل `abort(404)` بـ `courses.py:411`؛ باقي المراجعة الشاملة لم تبدأ |
| 7 | الاختبارات (تغطية ناقصة) | §15 | قيد التنفيذ | (جلسة سابقة) | 452/452 ناجح، لكن لا اختبار جديد يحمي إصلاح `abort(404)`؛ صلاحية `courses.manage` تحتاج تحقق من مصدر الحقيقة قبل اعتمادها نهائي |

### P2 — متوسط (واجهة / جودة كود)

| # | المجال | القسم بالبرومبت | الحالة | آخر تحديث | ملاحظات |
|---|---|---|---|---|---|
| 8 | الواجهة العامة (UI/UX) | §11 | لم يبدأ | — | — |
| 9 | العربية و RTL تحديداً | §12 | لم يبدأ | — | — |
| 10 | التصميم المتجاوب (Responsive) | §13 | لم يبدأ | — | — |
| 11 | الوصولية (Accessibility) | §14 | لم يبدأ | — | — |
| 12 | جودة الكود العامة | §18 | قيد التنفيذ | (جلسة سابقة) | حُذف قالب يتيم وملف tmp؛ باقي المراجعة (تكرار منطق، تسمية) لم تبدأ |

---

## قرارات معلّقة تحتاج قرارك أنت (لا تُحسم من الوكيل تلقائياً)

- **تشديد CSP فعلياً (مؤجَّل عن قصد)**: `script-src` ما زال
  فيه `'unsafe-inline' 'unsafe-eval'`، وهذا يعني أن حماية XSS عبر الـ CSP
  **غير فعّالة عملياً** اليوم. السبب مُقاس لا مُخمَّن: Tailwind من
  `cdn.tailwindcss.com` (مُترجم JIT، يحتاج `unsafe-eval`)، و146 معالج حدث
  `on*` مضمّن + 6 كتل `<script>` مضمّنة عبر 33 قالباً (يحتاج
  `'unsafe-inline'`). الإصلاح = استضافة Tailwind محلياً + نقل المعالجات
  المضمّنة إلى `static/js`. هذا عمل §11/§18 لا تغيير هيدر. **القرار المطلوب:**
  هل تستثمر به، أم تُبقي CSP واسعاً وتقبل أن XSS مانعه `bleach` + autoescape
  فقط (`security/sanitize.py` موجود ويعمل)؟

- **Cloudflare / WAF على الحافة**: مستحيل حالياً — يحتاج دومين خاصك + خطة
  PythonAnywhere مدفوعة. **ليس نقصاً تقنياً في الحين**؛ طبقة التطبيق فوقه
  تُبنى بدونه. إن اشتريت دومين لاحقاً، الاثنان يتكاملان ولا يتعارضان.

- **`FLASK_DEBUG=off` فخّ في Flask 3.1**: مجموعة القيم الكاذبة في Flask هي
  `{'0','false','no',''}` — `'off'` **غير موجودة**، يعني `FLASK_DEBUG=off`
  **يشغّل** الـdebugger. مُغطّى باختبار. على PythonAnywhere: امسح المتغير
  ولا تحاول "تعطيله" بـoff.

- **مصير تبويب أكواد المقررات المدمج** (`{% if false %}` بـ `list.html:154`) —
  يُحذف نهائياً، أو يُفعَّل؟ لم يُحسم بعد.
- **صلاحية `courses.manage`**: `research_development` أو `faculty_affairs`؟
  الجلسة السابقة غيّرت الـ fixtures لتطابق الكود الحالي — لم يُتحقق من
  مواصفة الأدوار الأصلية (`TEACHER_ROLE_FINAL_SPEC.md`) لمعرفة أيهما صحيح فعلياً.
- **محتوى مملوك لمستخدم محذوف** (من مجال #1، اكتُشف 2026-09-26):
  `course_content_submissions` صف 1 في `database/data.db` (مسودة منشورة
  «أجهزة قياس كهربائية» / ت209، لها صف منهاج) يشير إلى `users.id = 1`
  و`reviewed_by = 1` والمستخدم غير موجود — `users` تبدأ من `id = 2`.
  `PRAGMA foreign_key_check` يرجع المخالفة. **لا يمكن حذف السجل**: هو محتوى
  حقيقي منشور، و`user_id` `NOT NULL` فلا يصلح تصفيره. الأثر تجميلي فقط
  (العرض ي LEFT JOIN على users فيظهر الاسم فارغاً — `page_routes/teacher_pages.py:1465`).
  تحتاج قرارك: تُنسب إلى مستخدم قائم، أم تُرك كما هي؟ الكود لم يتغيّر.
- **تصحيح عدّ الاختبارات (تحديث 2026-09-26، مرتين)**: الرقم الصحيح الآن
  **534** لا 452/458 ولا 513. أساس المجال #1 كان ناقصاً 52 اختباراً (فات ملف
  `tests/test_security_headers.py` بالكامل)، ورقم 513 الذي سجّلته كان **ناقصاً
  5 أيضاً** — الأساس الحقيقي **518** (‏518 + 16 اختبار المجال #2 = 534).
  الخطأ واحد: سكربت الجمع يُسقط ملفات. **لا يوجد أي اختبار مفقود ولا إخفاق
  إطلاقاً** — الإجمالي 534/534 ناجح (exit 0). القاعدة: اقرأ
  `pytest tests --collect-only -q` وقت الجلسة، ولا تعتمد على رقم مكتوب في جلسة
  سابقة، وجمِع على مستوى **كل ملف** لا على عدّاد واحد.
- **`RESET_BASE_URL` يجب ضبطه في الإنتاج** (من مجال #2): أصل رابط إعادة تعيين
  كلمة المرور كان يُؤخذ من `Host` header الطلب، والرابط نفسه يحمل رمز استعادة
  يُستخدم كسند. أضفتُ `RESET_BASE_URL` (متغير بيئة) تُبنى منه الرابط عند
  ضبطه. تركه فارغاً = السلوك القديم + تحذير في السجلّ، حتى لا تنكسر بيئة
  التطوير. **القرار المطلوب**: اضبطه على نطاق الإنتاج الحقيقي
  (مثل `https://portal.example.edu`) قبل النشر.
- **`TRUSTED_PROXY_IPS`: CIDR أم نص حرفي؟** (من مجال #2): `serve.py:21` يوثّق
  `10.0.0.0/8`، لكن `core/wsgi_proxy.py:41` يطابق النص الحرفي، فأي شبكة CIDR
  موثّقة **لا تُطابق أبداً**. الاتجاه آمن (لا ثقة بالوسيط إطلاقاً ⇒ لا انتحال
  مصدر)، لكن عملياً كل المستخدمين يشاركون جدول معدِّل واحد ⇒ 5 محاولات فاشلة
  من أي مستخدم **تقفل تسجيل الدخول للجميع**. إمّا دعم CIDR، أو تصحيح التوثيق
  ليكون المثال `10.0.0.5`. **القرار 요청**.

---

## سجل الجلسات (يُضاف سطر جديد بنهاية كل جلسة، لا يُحذف القديم)

| التاريخ | المجال المنفَّذ | النتيجة | ملفات تغيّرت |
|---|---|---|---|
| (تاريخ الجلسة السابقة) | إصلاح فشل اختبارات (خارج نطاق هذا الجدول أصلاً) | 452/452 ناجح | `page_routes/courses.py`, `templates/courses/codes.html`, حذف `public/course_content.html`, عدة ملفات اختبار |
| 2026-09-26 | #1 قاعدة البيانات — اكتمل: SQLi و FKs مُدقَّقان، N+1 مُصلح، فهرس `timetable(teacher_id, day)` مُضاف | **513/513 ناجح** (exit 0) | `database/schema.sql`, `database/schema.py`, `services/course_content_service.py`, `services/course_service.py`, `services/exam_service.py`, `tests/test_db_area1_nplusone.py` (9 اختبارات) |
| 2026-09-26 | بنية تحتية للتدقيق (خارج جدول المجالات) — تنظيم الأدلة المرجعية الخارجية | 513/513 ناجح بعد التغيير | `GUIDES_MAP.md` (جديد), `_audit_guides/` (285 ملف / 1.43 ميجا), حذف `github-pages-main/` (8642 ملف / 257 ميجا), `CONTINUATION_PROTOCOL.md` |
| 2026-09-26 | #3 الدفاع الأمني العام — تدقيق وإصلاح XSS في الواجهة: 11 ملف JS، إصلاح سلسلة `flash` → toast، 3 حقول داخل سمات HTML، وتكميل `'` في escapers ناقصة | **575/575 ناجح** (38 اختباراً جديداً) | `static/js/toast.js`, `courses_list.js`, `teachers_assign.js`, `teachers_teaching_record_standalone_dept.js`, `faculty_performance_select_report.js`, `faculty_performance_edit_assignments.js`, `courses_create.js`, `timetable_live.js`, `exams/workspace.js`, `spa/app.js`, `shared/timetable_helpers.js`, `tests/test_xss_output_escaping.py` (جديد) |
| 2026-09-26 | #3 الدفاع الأمني العام — رؤوس الأمان نُقلت إلى `security/security_headers.py`، HSTS مُضاف، `X-XSS-Protection` محذوف، توجيهات CSP ناقصة مُضافة، قفل Debug، حجب فحوصات البوتات | 515/515 ناجح (52 اختباراً جديداً) | `security/security_headers.py` (جديد), `app.py`, `tests/test_security_headers.py` (جديد) |
| 2026-09-26 | #2 المصادقة والتفويض — اكتمل: حصر 255 مساراً (صفر غير محمي)، **مُصلح بقاء الصلاحيات بعد إبطالها** (5 مسارات لم تكن تدوّر `session_version`)، وجذر رابط الاستعادة (`RESET_BASE_URL`) | **534/534 ناجح** (exit 0، 16 اختباراً جديداً) | `database/repositories/user_repository.py`, `config.py`, `page_routes/auth_pages.py`, `tests/test_auth_area2_authorization.py` (جديد) |
| 2026-09-26 | #4 أمان منطق الأعمال — اكتمل **بنتيجة سالبة**: لا عزل مكسور بين الأقسام أو الملاك على أي سطح فُحص؛ لم يُعدَّل أي كود مصدري، بل ثُبِّتت الحدود بحارس انحدر | **586/586 ناجح** (exit 0، ١١ اختباراً جديداً، تحقّق عكسي ٦/٦) | `tests/test_business_logic_area4_isolation.py` (جديد) |
| 2026-09-26 | #3 الدفاع الأمني العام — أُغلق: حكم CSRF (٩ + ٦ اختبارات قائمة) وحكم IDOR (مُغطّى في #2 و#4) والإعدادات موثّقة، بلا تكرار في العمل | ٦٢٠ مجموعة؛ الإخفاقات كلها خارج نطاق المجال | `AUDIT_TRACKER.md` |
| 2026-09-26 | #5 أمان رفع الملفات — اكتمل **بنتيجة سالبة**: ٩ مواضع رفع، ٨ ضوابط متحقَّقة، ولا مسار ينتج محتوى نشطاً. **فجوة دفاع عميق موثّقة** (`/uploads/` بلا سياسة امتداد، وCSP فيه `unsafe-inline`). **علة سابقة خارج المجال اكتُشفت وأُصلحت**: ترتيب معكوس في `ensure_schema` كسر كل اختبار على قاعدة حديثة | ملف المجال: **٢٦ ناجح + ١ xfailed** (٢٧ اختباراً جديداً، تحقّق عكسي ١٢/١٢). كامل المجموعة: ٦٠٦ ناجح / ١٣ فاشل / ١ xfailed من ٦٢٠ | `tests/test_file_upload_area3.py` (جديد), `database/schema.py`, `tests/test_business_logic_area4_isolation.py`, `AUDIT_TRACKER.md` |

### دروس أدوات من #5 — الحارس الذي لا يُحمل يمرّ بصمت

التحقق العكسي في #5 (١٢/١٢) أوقف **حارسين زائفين كانا سينجحان دائماً**، وكلاهما من النوع الذي يُكتب بسرعة ثم يُقرأ على أنه حماية:

1. **طرد النقاط من الجهة الخطأ**: في `EXECUTABLE_EXTENSIONS & _PHOTO_EXTENSIONS` فُسِّر أن عناصر المجموعة الأولى بلا نقاط بينما امتدادات الثانية بنقاط، فكُنيت دالة `lstrip('.')` على المجموعة الخاطئة، فصار **التقاطع فارغاً دائماً**، والاختبار «يمرّ» مهما أضاف أحد امتداداً نشطاً. **القاعدة**: بعد أي تنظيف على نص، اطبع الناتج على قيمة معروفة؛ لا تكتفِ بأن التقاطع «نظيف».
2. **محلّل نسي علامات التنصيص**: القوائم هنا بثلاث صيغ مختلفة، فأنتج المحلّل `"'html'"` التي لا تطابق `"html"` أبداً. **القاعدة**: اختبر حارسَ التحليل على حارس مُعدَّل، لا على السليم وحده.
3. **لا تفصل سياسة الإدخال عن سياسة الإخراج**: الحكم بأن قوائم الرفع نظيفة كان صحيحاً، لكن الاكتشاف الحقيقي (فجوة `/uploads/` و`unsafe-inline`) لم يكن ممكناً إلا بقراءة سياسة الخدمة **مع** سياسة الرفع في 테이블 نفسه.

### دروس أدوات من #4 — اقرأها قبل أي تحليل ساكن

أربعة مُحقِّقات أنتجت إشارات زائفة، وكل واحد كان سيولّد بلاغاً كاذباً أو غياباً كاذباً:

1. **مقارنة حالة الأحرف** طابقت `FROM teachers` (صغير) داخل نص SQL مُحوَّل لكامل، فلم يجد ولا استعلاماً واحداً على جدول نطاق — وأعاد «صفر ثغرة».
2. **كاشف `deleted_at`** افترض العمود في `schema.sql`، وهو يُضاف وقت التشغيل عبر `_safe_add_column` (`database/schema.py:2554`)؛ ف bloated قائمة بـ١٢٦ «تجاوز تومب» كانت ضجيجاً (الجداول العشر المُدرَجة كلها بلا `deleted_at` أصلاً).
3. **كاشف نطاق الطبقة العليا** بحث عن `department_id` حرفياً في المتن، فلم يرَ تفويض النطاق إلى `_check_teacher_access` — وكاد يبلّغ عن IDOR في مسارَّي طباعة مُحصَّنين.
4. **كاشف الصلاحيات على الديكور** أخطأ في استخراج الديكور وقرأ دالة مجاورة، فلمّا لاحظتُ أن `api_teacher_delete` محروسٌ بـ`teachers.view` (صلاحية قراءة تحرس كتابة!) ظننتُه ثغرة عالية؛ القراءة المباشرة للمصدر أثبتت أنه `@api_permission_required('teachers.manage')` صحيح.

**القاعدة المستفادة**: التحليل الساكن يُنتج **مرشّحين** لا نتائج، وكل مُحقِّق يُتحقَّق أولاً على جسد سليمة معروفة (كما تحققتُ من المُحقِّق مباشرة من مُحقِّق `.view` فوجدته يصنّف `rooms_list` غير مُعدِّل بينما هو ينشئ قاعات عبر خدمة). **الطبقة الموثوقة الوحيدة هي المسار الحقيقي**: استعلام مُنفَّذ، أو اختبار HTTP بمستأجرين وهميين، أو قراءة المصدر. وكذلك **كل حارس يجب أن يُثبت حمله**: التحقق العكسي (حذف الحماية ⇒ فشل الاختبار) هو الفرق بين حارس وخط يُمرّ.

> **ملاحظة على العدّ**: الرقم في هذا السطر (515) أكبر من 513 المذكور في السطر
> فوقه، وكلاهما صحيح في وقته. المشروع يُعدَّل بشكل متزامن أثناء الجلسات
> (تعديلات على `tests/` من جلسة أخرى)، فعدّ الاختبارات ينزلق. القاعدة:
> اقرأ `pytest tests --collect-only -q` وقت الجلسة، ولا تعتمد على رقم
> مكتوب في جلسة سابقة.
