# CONTINUATION_PROTOCOL.md

يُضاف هذا كملحق لـ `MASTER_PROJECT_IMPROVEMENT_PROMPT.md` — لا يستبدله.

يحل مشكلة واحدة تحديداً: كيف نضمن إن الوكيل يكمل التدقيق الشامل
عبر عدة جلسات، بدل ما يحاول "يفعل كل شي" بجلسة وحدة ويمر سطحياً على
معظمه.

---

## القاعدة الأساسية

**كل جلسة = مجال واحد فقط من `AUDIT_TRACKER.md`.**
لا استثناء، حتى لو الوكيل انتهى مبكراً أو رأى "تحسين سريع" بمجال ثاني.

---

## بداية كل جلسة — بهذا الترتيب بالضبط

1. اقرأ `AUDIT_TRACKER.md` كاملاً أولاً.
2. اقرأ قسم "قرارات معلّقة تحتاج قرارك أنت" — إذا فيه قرار لم يُحسم
   ويمس المجال اللي راح تشتغل عليه، توقف واسأل قبل ما تكمل.
3. اختر أول مجال بحالة "لم يبدأ"، بترتيب الأولوية (P0 كامل قبل P1،
   P1 كامل قبل P2) — إلا إذا كان فيه مجال "قيد التنفيذ" من جلسة سابقة
   بنفس مستوى الأولوية أو أعلى، فهذا يُكمَّل أولاً قبل مجال جديد.
4. اقرأ القسم المطابق بـ `MASTER_PROJECT_IMPROVEMENT_PROMPT.md` (بالرقم
   المذكور بعمود "القسم بالبرومبت").
5. طبّق القواعد الدائمة (قسم "قواعد عمل دائمة" بأعلى `AUDIT_TRACKER.md`)
   طول الجلسة — هذي ثابتة، تُطبَّق دائماً بغض النظر عن المجال.
6. ابدأ العمل على هذا المجال فقط.

---

## أثناء الجلسة

- إذا اكتشفت مشكلة تخص مجال غير المجال الحالي: **سجّلها بعمود
  "ملاحظات" لذلك المجال بـ `AUDIT_TRACKER.md`**، ولا تُصلحها الآن.
  مثال: أثناء مراجعة قاعدة البيانات (§6) تكتشف خلل CSRF — هذا يخص §8،
  يُسجَّل بملاحظة، لا يُصلَح بنفس الجلسة.
- إذا القرار يمس متطلب موجود أو يغيّر سلوك ظاهر للمستخدم: أضفه لقسم
  "قرارات معلّقة تحتاج قرارك أنت" بدل ما يُحسم تلقائياً.

---

## نهاية كل جلسة — إلزامي، بدون استثناء

قبل ما تعتبر الجلسة منتهية، حدّث `AUDIT_TRACKER.md`:

1. غيّر حالة المجال (لم يبدأ ← قيد التنفيذ ← منتهي)
2. إذا لم يكتمل المجال بالكامل: اكتب بالضبط أين توقفت (اسم ملف، رقم
   سطر، أو خطوة محددة) — الجلسة الجاية تكمل من هذي النقطة حرفياً، لا
   تبدأ المجال من الصفر
3. أضف سطر جديد بجدول "سجل الجلسات" — لا تحذف السطور القديمة
4. إذا اكتشفت قرارات معلّقة جديدة تحتاج مراجعة المستخدم، أضفها لقسمها

**تقرير نهاية الجلسة يتبع تنسيق القسم 22 بالبرومبت الأصلي**، لكن مربوط
مباشرة بالتحديث اللي صار بـ `AUDIT_TRACKER.md` — مو تقرير منفصل يضيع.

---

## الرسالة اللي ترسلها لبدء أي جلسة جديدة

انسخ هذا حرفياً بأول رسالة لأي جلسة جديدة (Cursor / Claude Code):

```
اقرأ AUDIT_TRACKER.md و CONTINUATION_PROTOCOL.md أولاً قبل أي شي.
اتبع القواعد فيهم بدقة: مجال واحد فقط بهذي الجلسة، حسب الأولوية
المذكورة بالجدول. حدّث AUDIT_TRACKER.md بنهاية الجلسة إلزامياً حتى
لو المجال لم يكتمل بالكامل.
```

وبعدها مباشرة، سطر واحد إضافي:

```
عند فتح أي دليل خارجي، استخدم GUIDES_MAP.md فقط لتحديد المسار — لا تتصفح
مجلد github-pages-main الكامل.
```

هذي الرسالة ثابتة — نفسها بكل جلسة، بغض النظر عن أي مجال راح يُنفَّذ،
لأن `AUDIT_TRACKER.md` نفسه هو اللي يحدد أي مجال الدور عليه.

**الأدلة الخارجية:** كل دليل مرجعي موجود الآن داخل `_audit_guides/`،
و`GUIDES_MAP.md` هو الفهرس الوحيد المسموح بقراءته لتحديد أي مسار. لا تفتح
`github-pages-main/` أبداً — هو 4005 ملف غير ذي صلة.
